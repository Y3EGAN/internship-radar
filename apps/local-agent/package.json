{
  "name": "@internship-radar/local-agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests && node --test ../../scripts/preparation-bridge.test.mjs",
    "typecheck": "tsc --noEmit",
    "radar": "tsx src/index.ts"
  },
  "devDependencies": {
    "playwright": "1.62.0"
  }
}
