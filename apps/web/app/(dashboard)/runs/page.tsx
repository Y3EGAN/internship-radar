import { requireOwner } from "../../../lib/auth";
export default async function RunsPage(){const {client}=await requireOwner();const {data}=await client.from("source_runs").select("id,started_at,duration_ms,outcome,attempted_count,succeeded_count,failed_count,discovered_count").order("started_at",{ascending:false}).limit(50);const rows=data??[];return <><header className="page-header"><div><p className="eyebrow">AUTOMATION HISTORY</p><h1>Runs</h1></div></header><table className="data-table"><thead><tr><th>Started</th><th>Outcome</th><th>Sources</th><th>Jobs</th><th>Duration</th></tr></thead><tbody>{rows.map(run=><tr key={run.id}><td>{new Date(run.started_at).toLocaleString("en-CA")}</td><td><span className={`badge ${run.outcome!=="succeeded"?"warning-state":""}`}>{run.outcome}</span></td><td>{run.succeeded_count}/{run.attempted_count} succeeded</td><td>{run.discovered_count}</td><td>{run.duration_ms===null?"Running":`${run.duration_ms} ms`}</td></tr>)}</tbody></table></>}
