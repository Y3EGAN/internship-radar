import { requireOwner } from "../../../lib/auth";
export default async function SourcesPage(){const {client}=await requireOwner();const {data}=await client.from("source_endpoints").select("id,ats,board_identifier,state,failure_count,last_checked_at,next_due_at,companies(name)").order("state").order("next_due_at").limit(100);const rows=data??[];return <><header className="page-header"><div><p className="eyebrow">REGISTRY HEALTH</p><h1>Sources</h1></div></header><table className="data-table"><thead><tr><th>Source</th><th>ATS</th><th>Health</th><th>Failures</th><th>Next due</th></tr></thead><tbody>{rows.map(row=><tr key={row.id}><td><strong>{row.board_identifier}</strong></td><td>{row.ats}</td><td><span className={`badge ${row.state!=="healthy"?"warning-state":""}`}>{row.state}</span></td><td>{row.failure_count}</td><td>{new Date(row.next_due_at).toLocaleString("en-CA")}</td></tr>)}</tbody></table></>}
