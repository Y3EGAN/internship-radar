import { requireOwner } from "../../../../lib/auth";
import { PairDevice,RevokeDevice } from "./device-actions";

export default async function DevicesPage(){const {client}=await requireOwner();const {data}=await client.from("device_tokens").select("id,device_label,last_used_at,expires_at,revoked_at").order("created_at",{ascending:false});const rows=data??[];return <><header className="page-header"><div><p className="eyebrow">COMPANION SECURITY</p><h1>Devices</h1></div></header><PairDevice/>{rows.length===0?<p className="empty">No local companion is paired.</p>:<table className="data-table"><thead><tr><th>Device</th><th>Last used</th><th>Expires</th><th>Status</th><th>Action</th></tr></thead><tbody>{rows.map(device=><tr key={device.id}><td>{device.device_label}</td><td>{device.last_used_at??"Never"}</td><td>{device.expires_at}</td><td><span className="badge">{device.revoked_at?"Revoked":"Active"}</span></td><td>{device.revoked_at?"—":<RevokeDevice tokenId={device.id}/>}</td></tr>)}</tbody></table>}</>}
